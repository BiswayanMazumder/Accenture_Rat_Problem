# contributions

```python
  for i in range(1, 365): # controls the number of days to commit starting from today
    for j in range(0, randint(1, 5)): # controls the number of commits for a single day in a random number
```
